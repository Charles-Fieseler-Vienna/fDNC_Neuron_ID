from setuptools import setup

setup(name='fDNC',
      version='0.2',
      description='Using Transformers for Whole Brain Freely Moving neuron tracking',
      author='Xinwei Yu',
      license='MIT',
      packages=setuptools.find_namespace_packages(),
      zip_safe=False)
